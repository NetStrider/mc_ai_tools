# mc_ai_tools - quick overview

This repo contains two top-level components:

- `mcai_agent` — the runtime agent and bridge adapters
- `mcai_world` — the world toolkit for NBT/anvil/chunk operations

Run the sample agent (stub):

python -m src.mcai_agent.runners.run_agent --config agent_config.example.json

